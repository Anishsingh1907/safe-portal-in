# For unified Vercel deployment, leave VITE_API_BASE_URL empty or unset (defaults to /api)
# For local development with Vite dev server, it automatically proxies /api to http://localhost:5000
# For separate backend deployment (e.g. Render), set: https://your-backend-api.onrender.com/api
VITE_API_BASE_URL=
